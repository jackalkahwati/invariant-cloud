{
  "name": "@coherence-engine/fixture-app",
  "version": "1.0.0",
  "description": "Benchmark fixture app — realistic web app target for the parallel runtime benchmark",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^20.11.5",
    "@types/supertest": "^7.2.0",
    "supertest": "^7.2.2",
    "typescript": "^5.4.2",
    "vitest": "^1.4.0"
  }
}
